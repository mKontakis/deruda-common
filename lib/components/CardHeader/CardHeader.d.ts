/// <reference types="react" />
import CardHeaderProps from "./CardHeaderProps";
declare const CardHeader: ({ className, children, color, plain, image, contact, signup, stats, icon, text, ...rest }: CardHeaderProps) => JSX.Element;
export default CardHeader;
